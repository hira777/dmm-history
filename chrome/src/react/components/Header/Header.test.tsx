import React from 'react';
import { render, fireEvent } from '@testing-library/react';
import '@testing-library/jest-dom/extend-expect';

import { Header } from './index';

describe('Header', () => {
  // TODO: 何をテストすれば良いのかすぐに思いつかないので後ほど対応
  it('', () => {
    expect(true).toBeTruthy();
  });
});
